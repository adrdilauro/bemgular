export interface BemgularConfig {
  block?: string;
  modifiers?: string[];
  feature?: boolean;
  isolated?: boolean;
}

export interface BemgularInternalConfig {
  block: string;
  modifiers: string[];
  feature: boolean;
  isolated: boolean;
}
